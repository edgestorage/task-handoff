{
  "name": "@task-handoff/desktop-shell",
  "version": "1.0.0",
  "private": true,
  "main": "src/main.cjs",
  "scripts": {
    "start": "node node_modules/electron/cli.js .",
    "build": "node --check src/config.cjs && node --check src/main.cjs && node --check src/preload.cjs",
    "check": "pnpm run build",
    "test": "node --test"
  },
  "dependencies": {
    "@task-handoff/protocol": "workspace:*",
    "electron": "catalog:",
    "electron-updater": "^6.8.9",
    "ws": "^8.21.0"
  }
}
