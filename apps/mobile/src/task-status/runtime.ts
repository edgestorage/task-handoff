import type { TaskStatusSurfacesState } from './model';

export async function syncTaskStatusSurfaces(_state: TaskStatusSurfacesState, _url: string) {
  // iOS provides the WidgetKit-backed implementation in runtime.ios.ts.
}
