{
  "name": "@task-handoff/controlled-instance-image",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('controlled-instance CLI is built by the root runtime package pipeline')\"",
    "check": "pnpm run build",
    "test": "node --test"
  },
  "dependencies": {
    "@task-handoff/controlled-instance": "workspace:*",
    "@task-handoff/core": "workspace:*",
    "commander": "^14.0.3"
  }
}
