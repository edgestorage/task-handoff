{
  "name": "@task-handoff/cli",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "pnpm run build",
    "test": "node --test"
  },
  "dependencies": {
    "@task-handoff/control-plane": "workspace:*",
    "@task-handoff/controlled-instance": "workspace:*",
    "@task-handoff/core": "workspace:*",
    "commander": "^14.0.3",
    "semver": "^7.8.5"
  }
}
